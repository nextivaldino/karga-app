import { SYNC_INK } from '@/modules/sync/syncVisual';

// Identidade visual de uma "Lista" — amarelo pastel bem mais claro do
// que o amarelo das cargas/sync (`SYNC_HEADER_BG`), para não se
// confundir com os cartões de sincronização apesar de partilhar a
// mesma família de cor.
export const LISTA_BG = '#fff4cc';
export const LISTA_BG_HOVER = '#ffecb0';
export const LISTA_INK = SYNC_INK;
